It allows working with dynamically generated embedded forms in main form.

It supports embedded forms that are based on relation (Doctrine) as well as user defined forms 
that are not connected to any model